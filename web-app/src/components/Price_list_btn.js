import React from "react";

export default function Price_list_btn(props) {
    return(
        // функионала пока нет(и будет ли)
        <button className = "price_list_btn">
            Прайс лист
        </button>
    )
}